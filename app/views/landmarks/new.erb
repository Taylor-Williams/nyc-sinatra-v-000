<h2>Create New Landmark</h2>

<form class="create_landmark" action="/landmarks" method="post">
  <label for="landmark_name">landmark Name:</label>
  <input type="text" name="landmark[name]" id="landmark_name">
  <br>
  <p>does your landmark have any of these titles?</p>
  <%@titles.each do |title|%>
  <input type="checkbox" name="landmark[title_ids][]" value="<%=title.id%>" id="title_<%=title.id%>">><%=title.name%>
  <% end %>
  <br>
  <p>does your landmark have any of these landmarks?</p>
  <%@landmarks.each do |landmark|%>
  <input type="checkbox" name="landmark[landmark_ids][]" value="<%=landmark.id%>" id="landmark_<%=landmark.id%>"><%=landmark.name%>
  <% end %>
  <br>
  <label for="landmark_landmark">Create a new landmark for your Landmark:</label>
  <input type="text" name="landmark[name]" id="new_landmark">
  <label for="landmark_title">Create a new title for your Landmark (make multiple by comma separating them):</label>
  <input type="text" name="title[name]" id="new_title">
  <input type="submit" value="Create New Landmark">
</form>

<a class="button" href="/figures">Landmarks</a>
<a class="button" href="/">Home</a>
